# Teaching Notes

- User is an experienced Rails developer — no basic Rails usage explanations needed
- Workspace lives at rails-internals/ (not the working dir root) — user preference from session 1
- Lesson order: Rack spec → Rack middleware → ActionDispatch → ActionController → Routing (journey gem) → Railties → Engines → Contributing
