# Notes

- User is building a CLI program in Go; yaml.v3 is the runtime. Spec edge cases that affect Go unmarshalling are highest priority.
- User wants full-spec coverage, not just the common subset — don't skip obscure features.
- Frame lessons around config file design decisions, not abstract YAML theory.
